---
export interface Props { class?: string; height?: number }
const { class: className = '', height = 36 } = Astro.props;
const width = Math.round(height * 4.17);
---
<a href="/" class={`inline-flex items-center ${className}`} aria-label="UK IPTV BOX, home">
  <img
    src="/logo.png"
    alt="UK IPTV BOX logo"
    width={width}
    height={height}
    style={`height:${height}px;width:auto`}
    loading="eager"
    decoding="async"
  />
</a>
